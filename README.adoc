= 🗂️ Tools for gematik Directory (Werkzeuge für TI Verzeichnisdienst)

.Authentisiert und speichert den ACCESS_TOKEN in die Umgebungsvariable 
[source=bash]
----
export ADMIN_ACCESS_TOKEN=`./vzd-cli --env=.env.ru admin auth`
----

